# 最终实现指南（Veo Flash + Pro，积分制）

## ✅ 已完成的核心更新

### 1. 积分消耗更新

- ✅ **Sora**: 10 credits / render
- ✅ **Veo Flash**: 50 credits / render（≈ 5×Sora）
- ✅ **Veo Pro**: 250 credits / render（≈ 25×Sora）

### 2. 模型名称统一

- ✅ 所有代码中的 `veo3.1-fast` → `veo-flash`
- ✅ 所有代码中的 `veo3.1-pro` → `veo-pro`
- ✅ API 层自动映射到实际 API 模型名称

### 3. 钱包系统数据库 Schema（048_add_credit_wallet_system.sql）

- ✅ `credit_wallet`: 永久积分 + Bonus 积分（带过期）
- ✅ `credit_ledger`: 所有加减分都落账
- ✅ `render_job`: 生成任务（用于风控与成本核算）
- ✅ `risk_profile`: 风控画像

### 4. 核心函数

- ✅ `get_total_available_credits()`: 获取总可用积分
- ✅ `can_purchase_starter()`: 检查是否可以购买 Starter
- ✅ `can_use_bonus_for_model()`: 检查 Bonus 是否可用于模型
- ✅ `deduct_credits_from_wallet()`: 扣除积分（优先 Bonus）
- ✅ `add_credits_to_wallet()`: 添加积分到钱包

### 5. 触发点组件（VeoUpgradeNudge.tsx）

- ✅ 4 种触发点：AFTER_SORA_2, REMIX_3, EXPORT_CLICK, BONUS_NEAR_EMPTY
- ✅ 无感升级文案
- ✅ 完整埋点追踪

## 📋 待完成的工作

### 1. 更新 API 路由中的模型验证

需要更新：
- `app/api/video/generate/route.ts` - 已更新模型枚举
- `app/api/video/result/[id]/route.ts` - 可能需要更新
- 其他使用模型的地方

### 2. 更新充值流程

需要在充值成功时：
- 识别充值档位（Starter $4.9, Creator $39, Studio $99, Pro $299）
- 调用 `add_credits_to_wallet()` 设置永久/Bonus 积分
- 如果是 Starter，设置 `starter_purchased_at` 和限制

### 3. 更新积分扣除逻辑

需要将现有的 `deductCredits()` 替换为：
- 使用 `deduct_credits_from_wallet()`（优先 Bonus）
- 检查 Bonus 是否可用于模型（Veo Pro 不能用 Bonus）

### 4. 更新定价页面

需要显示 4 档充值包：
- Starter Access (7 days) — $4.90
- Creator — $39 (Recommended)
- Studio — $99
- Pro — $299

## 🎯 核心策略

### 定位

- **Sora**: Everyday Creator Model（日常高频）
- **Veo Flash**: Quality Upgrade（更高质、仍然快）
- **Veo Pro**: Final Cut / Studio Grade（最终成片）

### 现金流目标

- **3 个 Creator 包/月** = 正现金流
- **18 次 Veo Pro/月** = 正现金流

### Starter 无薅点化

- 只能买 1 次 / 账号
- 同支付方式 / 设备指纹 / IP 段：只能触发 1 次
- Starter Bonus 仅可用于 Sora + Veo Flash（禁止 Veo Pro）
- 每 24 小时最多：Sora 6 次，Veo Flash 1 次

## 🚀 下一步执行

1. **执行数据库迁移**: `048_add_credit_wallet_system.sql`
2. **更新充值 API**: 识别档位并设置钱包
3. **更新积分扣除**: 使用新钱包系统
4. **更新定价页面**: 显示 4 档充值包
5. **测试完整流程**: 验证所有功能

## 💡 关键洞察

你现在已经不是在做「AI 视频工具」，而是在做：

**"视频预览层 + 成果升级层"的平台结构 + 智能增长系统 + 完整风控体系 + 钱包系统**

这在 2026 年是极少数人能想清楚的路径。

