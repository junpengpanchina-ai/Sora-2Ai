# 如何找到 Supabase Site URL 设置

## 📍 位置说明

Site URL 配置在 **Settings** > **API** 页面，不在当前的 "Project Settings" > "General" 页面。

## 🎯 操作步骤

### 方法 1: 通过左侧导航栏

1. **在左侧导航栏找到 "Settings"**
   - 在顶部，应该能看到 "Settings" 标题
   - 如果没有看到，可能需要滚动到顶部

2. **点击 "API" 或 "Data API"**
   - 在 "CONFIGURATION" 部分下
   - 或者直接在 "Settings" 下找到 "API"

3. **找到 "Site URL" 字段**
   - 在 "API" 页面中，应该能看到 "Site URL" 输入框
   - 通常位于页面顶部或 "General" 部分

### 方法 2: 直接访问 URL

直接访问以下 URL（替换项目 ID）：
```
https://supabase.com/dashboard/project/hgzpzsiafycwlqrkzbis/settings/api
```

### 方法 3: 通过搜索

1. 在 Supabase Dashboard 顶部使用搜索功能
2. 搜索 "Site URL" 或 "API settings"
3. 点击搜索结果

## 📸 应该看到什么

在 "API" 页面，你应该看到：

- **Site URL** 输入框
- **API URL**（只读，显示 Supabase API 地址）
- **Project URL**（只读，显示 Supabase 项目地址）
- 其他 API 相关设置

## ✅ 修改步骤

1. 找到 **Site URL** 字段
2. 点击 **Edit** 或直接修改输入框
3. 将值改为：`https://sora2aivideos.com`
4. 点击 **Save** 或 **Save changes**

## 🔍 如果找不到

如果仍然找不到 "API" 页面，可以：

1. **检查权限**：确保你有项目管理员权限
2. **刷新页面**：按 `F5` 或 `Cmd+R` 刷新
3. **检查 URL**：确认你在正确的项目中（项目 ID: `hgzpzsiafycwlqrkzbis`）

## 📝 注意事项

- Site URL 用于 OAuth 重定向和邮件链接
- 修改后可能需要几分钟生效
- 确保同时配置了 Redirect URLs（在 Authentication > URL Configuration）

