# 🔧 Stripe Payment Link 编辑 - 详细操作步骤

## 根据你的截图，我看到你有这些 Payment Links：

- ✅ **$4.90** - "sora-ai-2 【99】" (激活)
- ✅ **$39.00** - "sora-ai-2 【39】" (激活)  
- ✅ **$99.00** - "sora-ai-2 【99】" (激活)
- ✅ **$299.00** - "sora-ai-2 【299】" (激活)

## 📝 编辑 Payment Link 的步骤（详细版）

### 方法 1: 通过列表直接编辑（推荐）

1. **在 Payment Links 列表页面**（你当前所在的页面）
2. **点击你要编辑的 Payment Link 名称**（比如点击 "sora-ai-2 【99】"）
   - 这会打开该 Payment Link 的详情页面

3. **在详情页面，你会看到多个标签/选项**：
   - 找到并点击 **"Settings"** 或 **"设置"** 标签
   - 或者找到 **"Checkout settings"** 或 **"结账设置"**

4. **滚动页面找到 "After payment" 部分**：
   - 可能显示为 **"After payment"**（支付后）
   - 或 **"Redirects"**（重定向）
   - 或 **"Success page"**（成功页面）

5. **设置 Success URL**：
   - 在 **"Success URL"** 或 **"Redirect URL"** 字段中粘贴：
   ```
   https://sora2aivideos.com/billing/success?session_id={CHECKOUT_SESSION_ID}
   ```

6. **设置 Cancel URL**：
   - 在 **"Cancel URL"** 字段中粘贴：
   ```
   https://sora2aivideos.com/pricing?canceled=1
   ```

7. **保存**：
   - 点击页面底部的 **"Save"** 或 **"保存"** 按钮

### 方法 2: 通过右侧菜单编辑

1. **在 Payment Links 列表页面**
2. **点击你要编辑的 Payment Link 右侧的 "..."（三个点）图标**
3. **从下拉菜单中选择 "Edit" 或 "编辑"**
4. **按照方法 1 的步骤 3-7 继续**

### 方法 3: 如果找不到 "After payment" 选项

某些 Payment Links 可能使用不同的设置方式：

1. **点击 Payment Link 进入详情页**
2. **查找以下可能的选项**：
   - **"Checkout customization"**（结账自定义）
   - **"Redirects"**（重定向）
   - **"Post-payment"**（支付后）
   - **"Success page"**（成功页面）
   - **"Advanced settings"**（高级设置）

3. **如果还是找不到，尝试**：
   - 点击页面右上角的 **"Edit"** 或 **"编辑"** 按钮
   - 查看是否有 **"More settings"** 或 **"更多设置"** 选项

## 🎯 针对你的 4 个 Payment Links 的具体操作

### Payment Link 1: $4.90

1. 点击列表中的 **"sora-ai-2 【99】"**（价格为 $4.90 的那个）
2. 进入详情页 → **"Settings"** → 找到 **"After payment"**
3. 设置 Success URL 和 Cancel URL
4. 保存

### Payment Link 2: $39.00

1. 点击列表中的 **"sora-ai-2 【39】"**
2. 重复上述步骤

### Payment Link 3: $99.00

1. 点击列表中的 **"sora-ai-2 【99】"**（价格为 $99.00 的那个）
2. 重复上述步骤

### Payment Link 4: $299.00

1. 点击列表中的 **"sora-ai-2 【299】"**
2. 重复上述步骤

## 🔍 如果仍然找不到设置选项

### 选项 A: 创建新的 Payment Link（推荐）

如果旧的 Payment Link 界面不支持设置重定向，可以创建新的：

1. **点击页面右上角的 "+ 创建支付链接 N" 按钮**
2. **设置基本信息**：
   - 名称：如 "Starter Access - $4.90"
   - 价格：$4.90
   - 货币：USD

3. **在创建过程中，查找 "After payment" 或 "Redirects" 选项**
4. **设置 Success URL 和 Cancel URL**
5. **创建完成后，更新 `app/pricing/page.tsx` 中的链接**

### 选项 B: 使用 Stripe API 设置（高级）

如果 Dashboard 界面不支持，可以通过 API 设置：

```bash
# 需要安装 Stripe CLI 或使用 API
stripe payment_links update plink_xxx \
  --after-completion[type]=redirect \
  --after-completion[redirect][url]="https://sora2aivideos.com/billing/success?session_id={CHECKOUT_SESSION_ID}"
```

## 📸 界面位置参考

根据 Stripe Dashboard 的常见布局：

1. **Payment Link 详情页通常有这些标签**：
   - Overview（概览）
   - Settings（设置）← **在这里找**
   - Analytics（分析）
   - Logs（日志）

2. **Settings 标签下通常有这些部分**：
   - General（常规）
   - Pricing（定价）
   - Checkout customization（结账自定义）
   - **After payment**（支付后）← **在这里**
   - Advanced（高级）

## ⚠️ 重要提示

1. **域名替换**：如果你的域名不是 `sora2aivideos.com`，请替换为你的实际域名
2. **占位符保留**：`{CHECKOUT_SESSION_ID}` 必须保留，不要修改
3. **测试模式 vs 生产模式**：确保在正确的模式下设置（Test mode 或 Live mode）

## 🆘 如果还是找不到

请告诉我：
1. 点击 Payment Link 后，详情页显示了哪些标签/选项？
2. 在 Settings 标签下，你看到了哪些设置选项？
3. 是否有 "Redirects"、"After payment"、"Success page" 等类似的选项？

这样我可以给你更精确的指导。

