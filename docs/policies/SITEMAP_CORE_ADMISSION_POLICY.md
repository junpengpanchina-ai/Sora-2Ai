# Sitemap-Core 准入 Gate Policy

> **版本**: v1.0  
> **生效日期**: 2026-01-24  
> **状态**: 🔴 LOCKED（当前 sitemap-core 未进入 index）

---

## 一句话版本

```
sitemap-core.xml 只有在 Tier1 Index Rate 稳定达标后，
才允许进入 sitemap index。
```

---

## 设计哲学

```
Tier1 是探针，Core 是资产。
资产不能在探针失灵前暴露。
```

---

## Gate Policy 正式定义

### 前置对象

| 项目 | 值 |
|------|-----|
| **观察池** | `tier1-0.xml` |
| **评估指标** | Index Rate（Indexed / Discovered） |
| **观察窗口** | 连续 14 天 |

---

## 准入判定条件

### 🚦 ALL must pass

| # | 条件 | 硬性要求 |
|---|------|----------|
| 1 | **Index Rate** | ≥ 65% |
| 2 | **稳定性** | 连续 7 天波动 ≤ ±5% |
| 3 | **Crawl Error** | 无系统性错误（非 5xx / 非 Canonical 异常） |
| 4 | **抓取趋势** | Crawl Stats 非下降趋势 |
| 5 | **人工抽检** | ≥ 5 URL，均为 Eligible / Indexed or Crawled |

### 判定逻辑

```
IF condition_1 AND condition_2 AND condition_3 AND condition_4 AND condition_5:
    ALLOW: ADD sitemap-core.xml INTO sitemap.xml index
ELSE:
    DENY: sitemap-core remains outside index
```

---

## 明确禁止行为（Hard NO）

```
❌ Index Rate 未评估就合并 sitemap
❌ 因"URL 不多 / 看起来重要"提前加 core
❌ 用 sitemap-core 修复 tier1 Index Rate 偏低
❌ 在 tier1-1 尚未启用时引入第二组变量
❌ 任何形式的"这次特殊"例外
```

---

## 执行动作

### 当条件全部满足时

```bash
# 修改 app/sitemap.xml/route.ts
# 将 sitemap-core.xml 加入 sitemapindex

const sitemaps = [
  { loc: `${baseUrl}/sitemap-core.xml`, lastmod: now },  // 新增
  ...tier1Sitemaps,
];
```

### 当条件未满足时

```
保持现状：
- sitemap-core.xml 独立存在
- 不在 sitemap.xml index 中引用
- 可通过内链自然被发现
```

---

## 状态追踪

| 日期 | Index Rate | 稳定性 | 状态 | 决策 |
|------|------------|--------|------|------|
| 2026-01-24 | 未知 | 未知 | 🔴 观察期 | 不动 |
| Day 7 | - | - | - | 待填写 |
| Day 14 | - | - | - | 待填写 |

---

## 审计日志模板

当执行准入决策时，必须记录：

```markdown
## sitemap-core 准入决策记录

**日期**: YYYY-MM-DD
**决策人**: [姓名]
**决策**: ALLOW / DENY

### 条件检查

| 条件 | 要求 | 实际值 | 通过 |
|------|------|--------|------|
| Index Rate | ≥ 65% | XX% | ✅/❌ |
| 稳定性 | ≤ ±5% | XX% | ✅/❌ |
| Crawl Error | 无 | X 个 | ✅/❌ |
| 抓取趋势 | 非下降 | 上升/稳定/下降 | ✅/❌ |
| 人工抽检 | 5 URL | X/5 通过 | ✅/❌ |

### 最终决策

- [ ] 所有条件通过 → 执行合并
- [ ] 存在未通过条件 → 继续观察
```

---

## 与其他 Policy 的关系

| Policy | 关系 |
|--------|------|
| SEO Scaling Gate | 本 Policy 是 Scaling Gate 的子集 |
| No Override Rule | Gate BLOCKED 时不允许例外 |
| 14-Day Playbook | 观察窗口对齐 |

---

*Policy 版本: 1.0 | 创建时间: 2026-01-24*
