# GSC URL 样例确认（已收到）

> **状态**：已收到 2 个真实 URL 样例，已优化 SQL 规则

---

## ✅ 已收到的 URL 样例

### 1. Use Cases 页面 URL
```
https://sora2aivideos.com/use-cases/anime-video-generator
```
- **状态**：404 Not Found（错误页面，但仍可作为样例）
- **路径模式**：`/use-cases/{slug}`
- **表名**：`use_cases`
- **字段**：`slug`
- **SQL 处理**：脚本会自动识别 404 → `tag: 'delete'`, `tag_reason: 'http_404'`

---

### 2. Keywords 页面 URL
```
https://sora2aivideos.com/keywords/ai-video-generator-free
```
- **状态**：500 Internal Server Error（错误页面，但仍可作为样例）
- **路径模式**：`/keywords/{slug}`
- **表名**：`long_tail_keywords`
- **字段**：`page_slug`（注意：不是 `slug`，且可能包含 `keywords-` 前缀）
- **SQL 处理**：脚本会自动识别 500 → `tag: 'keep'`, `tag_reason: 'server_500'`

---

### 3. Blog 页面 URL（待提供）
```
待提供
```
- **路径模式**：`/blog/{slug}`
- **表名**：`blog_posts`
- **字段**：`slug`

---

## 📝 说明

### 为什么错误页面仍可作为样例？

这些错误页面**完全可以用**作为 URL 样例，因为：

1. **路径结构正确**：
   - `/use-cases/anime-video-generator` → 路径格式正确
   - `/keywords/ai-video-generator-free` → 路径格式正确

2. **脚本会自动处理**：
   - 404 → `tag: 'delete'`, `tag_reason: 'http_404'`
   - 500 → `tag: 'keep'`, `tag_reason: 'server_500'`

3. **这正是 GSC 未收录 URL 的常见情况**：
   - 很多未收录的 URL 就是 404/500 错误页面
   - 脚本需要识别并分类这些错误

---

## 🔧 已优化的 SQL 规则

基于这两个 URL，已优化：

1. **Use Cases**：
   - ✅ 路径识别：`/use-cases/{slug}`
   - ✅ Slug 提取：`seg2`
   - ✅ 表名/字段：`use_cases.slug`

2. **Keywords**：
   - ✅ 路径识别：`/keywords/{slug}`
   - ✅ Slug 提取：`seg2`
   - ✅ 表名/字段：`long_tail_keywords.page_slug`
   - ✅ 前缀处理：支持 `page_slug` 可能包含 `keywords-` 前缀的情况

3. **Blog**（待确认）：
   - ✅ 路径识别：`/blog/{slug}`
   - ✅ Slug 提取：`seg2`
   - ✅ 表名/字段：`blog_posts.slug`

---

## 🎯 下一步

如果你有 Blog 页面的真实 URL（即使是错误页面也可以），请提供：
```
https://sora2aivideos.com/blog/xxx
```

拿到后我会进一步优化 SQL 规则，确保 100% 命中你的项目结构。

---

**相关文档**：
- [完整使用指南](./GSC_AUTO_LABELING_COMPLETE_GUIDE.md)
- [批量处理 SQL](./supabase/migrations/110_apply_gsc_tags_to_pages.sql)
