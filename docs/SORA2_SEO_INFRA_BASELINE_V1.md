# Sora2 SEO Infrastructure 基线版本 v1.0

> **文档版本**: 1.0 (Baseline)  
> **创建日期**: 2026-01-24  
> **适用身份**: 超体个体（Individual Operator）  
> **收款模式**: Stripe 一次性充值（Prepaid Credits）  
> **技术栈**: Next.js / Vercel / Supabase  
> **目标**: 可控扩容到 10 万页

---

## 目录

1. [核心原则](#一核心原则)
2. [事故复盘：Sitemap Off-by-One Bug](#二事故复盘sitemap-off-by-one-bug)
3. [分层 Sitemap 架构](#三分层-sitemap-架构)
4. [GSC 集成与监控](#四gsc-集成与监控)
5. [Index Health Dashboard](#五index-health-dashboard)
6. [自动化健康检查](#六自动化健康检查)
7. [SEO Scaling Gate](#七seo-scaling-gate)
8. [GSC URL 自动标签系统](#八gsc-url-自动标签系统)
9. [GSC 冷启动 14 天行动表](#九gsc-冷启动-14-天行动表)
10. [SEO 扩容 SOP](#十seo-扩容-sop)
11. [个人身份运营指南](#十一个人身份运营指南)
12. [Enterprise Preview 策略](#十二enterprise-preview-策略)
13. [法律文档体系](#十三法律文档体系)
14. [公司注册触发条件](#十四公司注册触发条件)
15. [数据库迁移清单](#十五数据库迁移清单)
16. [脚本与工具清单](#十六脚本与工具清单)
17. [文档索引](#十七文档索引)

---

## 一、核心原则

### 1.1 十条铁律

| # | 原则 | 说明 |
|---|------|------|
| 1 | SEO 是基础设施，不是内容问题 | 架构决定上限 |
| 2 | sitemap 空但合法会"静默失败" | Google 不报错，只是没东西发现 |
| 3 | tier1-0 是 sitemap 命门 | 必须从 0 开始，不能为空 |
| 4 | GSC 显示 0 不等于未抓取 | 要看 Index Rate，不是绝对数 |
| 5 | Index Rate 决定是否扩容 | 扩容前必须有基准线 |
| 6 | 扩容必须可回滚、可阻断 | Gate BLOCKED 时不允许 override |
| 7 | Enterprise 能力先以 Preview 运行 | 不强卖，筛选客户 |
| 8 | 个人身份 + credits 是最安全起步 | 最大灵活性，最低风险 |
| 9 | 不做 usage-based SEO 定价 | SEO 结果不可承诺 |
| 10 | 注册公司 = 责任切换而非升级 | 条件触发，不是情绪驱动 |

### 1.2 核心公式

```
Index Rate = Indexed / Crawled

健康值: > 70%
警戒值: 40-70%
危险值: < 40%
```

### 1.3 系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                    Sora2 SEO Infrastructure                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Sitemap 层                                                      │
│  ├── sitemap.xml (唯一入口)                                      │
│  │   ├── sitemap-core.xml    ← 核心页面（品牌锚点）                │
│  │   ├── tier1-0.xml         ← 高价值页面（扩容探针）              │
│  │   ├── tier1-1.xml                                            │
│  │   ├── tier2-0.xml         ← 扩容页面（试错池）                 │
│  │   └── ...                                                    │
│                                                                 │
│  监控层                                                          │
│  ├── Index Health Dashboard  ← 核心指标可视化                     │
│  ├── SEO Scaling Gate        ← CI/CD 扩容门控                    │
│  ├── Kill-Switch             ← 紧急熔断机制                      │
│  └── GSC Auto-Labeling       ← URL 自动分类                      │
│                                                                 │
│  数据层                                                          │
│  ├── seo_gsc_urls            ← GSC URL 状态表                    │
│  ├── seo_daily_metrics       ← 每日 SEO 指标                     │
│  ├── sitemap_chunks          ← Sitemap 分片状态                  │
│  └── v_seo_scaling_decision  ← 扩容决策视图                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、事故复盘：Sitemap Off-by-One Bug

### 2.1 问题描述

**现象**：
- GSC 显示 `/sitemap.xml` 状态为「成功」
- 但「已发现网页」= 0
- 看起来像 Google 没抓取

**根因**：
```
sitemap.xml 指向 tier1-1.xml（空）
而不是 tier1-0.xml（1000 URLs）
```

这是一个 **off-by-one 错误**：
- 路由从 `tier1-0` 开始
- 但 index 从 `tier1-1` 开始引用

### 2.2 错误代码

```typescript
// ❌ 错误代码
const tier1Sitemaps = Array.from({ length: tier1Chunks }, (_, i) => ({
  loc: `${baseUrl}/sitemaps/tier1-${i + 1}.xml`,  // 从 1 开始
  lastmod: now,
}))
```

### 2.3 修复代码

```typescript
// ✅ 修复代码
const tier1Sitemaps = Array.from({ length: tier1Chunks }, (_, i) => ({
  loc: `${baseUrl}/sitemaps/tier1-${i}.xml`,      // 从 0 开始
  lastmod: now,
}))
```

### 2.4 修复文件

| 文件 | 变更 |
|------|------|
| `app/sitemap.xml/route.ts` | `tier1-${i + 1}` → `tier1-${i}` |
| `app/sitemap-index.xml/route.ts` | `tier1-${i + 1}` → `tier1-${i}` |

### 2.5 验证命令

```bash
# 确认 index 指向 tier1-0
curl -s https://sora2aivideos.com/sitemap.xml | head -20

# 确认 tier1-0 有内容
curl -s https://sora2aivideos.com/sitemaps/tier1-0.xml | grep -c "<url>"

# 抽查 URL 可访问性
curl -I "https://sora2aivideos.com/use-cases/anime-video-generator"
```

### 2.6 核心教训

```
"成功" ≠ "有效"
Google 不会把"合法但空"的 sitemap 当错误
它只是没东西可发现
```

---

## 三、分层 Sitemap 架构

### 3.1 结构总览

```
sitemap.xml (唯一入口)
├── sitemap-core.xml     ← 核心页面（200-500）
├── tier1-0.xml          ← 高价值页面（1k/chunk）
├── tier1-1.xml
├── tier2-0.xml          ← 扩容页面（500/chunk）
├── tier2-1.xml
└── ...
```

### 3.2 各层定义

| 层级 | 目标 | URL 数/chunk | 收录目标 | canonical |
|------|------|-------------|---------|-----------|
| **Core** | 品牌信任 | < 500 | 100% | self |
| **Tier1** | 稳定收录 | 1,000 | 90%+ | self |
| **Tier2** | 扩容试错 | 500 | 30-70% | 可指向 Tier1 |

### 3.3 铁律

1. **Tier1 永远不指向 Tier2**
2. **Tier2 可以 canonical → Tier1**
3. **Index sitemap 只做导航，不做筛选**
4. **tier1-0 是 sitemap 的"命门"**

### 3.4 当前状态（2026-01-24）

```
sitemap.xml (index)
└── tier1-0.xml         ✅ 1,000 URLs

sitemap-core.xml        ⚠️ 276 URLs（刻意未在 index 中引用）
tier1-1.xml             ⚠️ 0 URLs（空，暂无数据）
```

### 3.5 sitemap-core 策略决策

**结论**：sitemap-core 暂不加入 index，这不是 Bug，而是正确的策略选择。

**理由**：
1. 需要建立 Index Rate 基准线
2. 必须保持单变量实验
3. sitemap-core 的角色本来就不是 Tier1
4. 符合 SEO Infra 铁律

---

## 四、GSC 集成与监控

### 4.1 GSC 实时数据（2026-01-20）

| 指标 | 值 | 评估 |
|------|-----|------|
| **已编入索引** | 29,000 | ✅ 非常好 |
| **未编入索引** | 1,126 | ⚠️ 需关注 |
| **Index Rate** | 29,000 / 30,126 = **96.3%** | 🟢 GREEN |

### 4.2 未编入索引原因分解

| 原因 | 数量 | 来源 | 优先级 |
|------|------|------|--------|
| 网页会自动重定向 | 708 | 网站 | 🟡 中 |
| 已发现 - 尚未编入索引 | 266 | Google 系统 | 🟢 正常 |
| 已抓取 - 尚未编入索引 | 100 | Google 系统 | 🟡 观察 |
| **服务器错误 (5xx)** | **43** | 网站 | 🔴 **必须修** |
| **未找到 (404)** | **8** | 网站 | 🔴 **必须修** |
| 备用网页（有规范标记） | 1 | 网站 | 🟢 正常 |

### 4.3 robots.txt

```
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/
Disallow: /auth/
Disallow: /storage-test/
Disallow: /payment-test/
Disallow: /prompts/

Sitemap: https://sora2aivideos.com/sitemap.xml
```

---

## 五、Index Health Dashboard

### 5.1 布局结构

```
┌────────────────────────────────────┐
│ Crawl Pipeline (Line)               │
│ Discovered / Crawled / Indexed      │
└────────────────────────────────────┘

┌───────────────┬────────────────────┐
│ Index Rate    │ Tier1 Sitemap Health│
│ (Line + SLA)  │ (Single Value 🚨)   │
└───────────────┴────────────────────┘

┌────────────────────────────────────┐
│ Index Delta (Daily Net Indexed)     │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ Duplicate & Soft 404 (Bar)          │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ Alerts Feed (Table)                 │
└────────────────────────────────────┘
```

### 5.2 核心指标

| 指标 | 公式 | 健康值 | 危险值 |
|------|------|--------|--------|
| **Index Rate** | Indexed / Crawled | > 70% | < 40% |
| **Index Rate 7d MA** | 7 日移动平均 | > 60% | < 45% |
| **Index Delta** | 今日 Indexed - 昨日 | > 0 | < 0 连续 3 天 |
| **Empty Chunks** | count(url_count = 0) | 0 | > 0 |
| **Duplicate Rate** | Duplicate / Indexed | < 10% | > 20% |
| **5xx Errors** | 服务器错误数 | 0 | > 10 |
| **404 in Sitemap** | Sitemap 中的 404 | 0 | > 0 |

### 5.3 Reason Code 体系

| Code | 含义 | 严重程度 |
|------|------|----------|
| `BLOCKED_TIER1_EMPTY` | tier1-0 为空 | FATAL |
| `BLOCKED_LOW_INDEX_RATE` | Index Rate < 40% | FATAL |
| `BLOCKED_INDEX_DELTA_NEGATIVE` | 连续 3 天负增长 | FATAL |
| `BLOCKED_HIGH_DUPLICATE` | Duplicate Rate > 20% | FATAL |
| `BLOCKED_5XX_ERRORS` | 5xx ≥ 10 (7d) | FATAL |
| `HOLD` | Index Rate 40-50% | WARNING |
| `CAUTIOUS` | Index Rate 50-70% | INFO |
| `SAFE_TO_SCALE` | 一切正常 | OK |

---

## 六、自动化健康检查

### 6.1 SQL 约束

```sql
-- 防止 tier1-0 为空
ALTER TABLE sitemap_chunks
ADD CONSTRAINT tier1_0_not_empty
CHECK (
  NOT (tier = 1 AND name = 'tier1-0' AND url_count = 0)
);
```

### 6.2 每日检查脚本

```typescript
// scripts/sitemap_health_check.ts
async function runCheck() {
  // 1. 检查 sitemap index
  const indexResponse = await fetch(`${BASE_URL}/sitemap.xml`);
  
  // 2. 检查各 chunk URL 数量
  for (const chunk of chunks) {
    const urlCount = await countUrls(chunk.url);
    if (urlCount === 0 && chunk.tier === 1) {
      throw new Error(`FATAL: ${chunk.name} is empty`);
    }
  }
  
  // 3. 更新数据库
  await updateSitemapChunks(results);
}
```

### 6.3 CI/CD Gate

```typescript
// scripts/seo-scaling-gate.ts
const decision = await supabase
  .from('v_seo_scaling_decision')
  .select('*')
  .single();

if (decision.decision === 'BLOCKED') {
  console.error(`❌ SEO scaling blocked: ${decision.reason_code}`);
  process.exit(1);
}
```

---

## 七、SEO Scaling Gate

### 7.1 Gate 规则

| 状态 | 条件 | 动作 |
|------|------|------|
| 🔴 **BLOCKED** | tier1_empty > 0 | 禁止扩容 |
| 🔴 **BLOCKED** | index_rate < 40% | 禁止扩容 |
| 🔴 **BLOCKED** | index_delta < 0 连续 3 天 | 禁止扩容 |
| 🔴 **BLOCKED** | duplicate_rate > 20% | 禁止扩容 |
| 🔴 **BLOCKED** | 5xx ≥ 10 (7d) | 禁止扩容 |
| 🟡 **HOLD** | index_rate 40-50% | 暂停扩容 |
| 🟡 **CAUTIOUS** | index_rate 50-70% | 谨慎扩容 |
| 🟢 **SAFE** | 以上都不满足 | 允许扩容 |

### 7.2 禁止 Override 条款

```
⛔ No manual override is allowed when SEO Gate is BLOCKED.

当 Gate 显示 BLOCKED 时，不允许：
- 手动跳过检查
- 修改阈值来"通过"
- 以"这次特殊"为由绕过

唯一的出路是：修复根因，让 Gate 自然变绿。
```

### 7.3 Kill-Switch 机制

**触发条件**：
- tier1-0 = 0
- Index Rate < 30%
- Indexed 突降 > 20%

**执行动作**：
1. 暂停所有内容生成
2. 冻结 sitemap 更新
3. 发送告警通知

### 7.4 回滚机制

```sql
-- 紧急 noindex Tier2
UPDATE pages 
SET meta_robots = 'noindex'
WHERE tier = 2 AND created_at > NOW() - INTERVAL '7 days';
```

---

## 八、GSC URL 自动标签系统

### 8.1 功能概述

一键将 GSC 导出的未收录 URL 自动分类为 `delete` / `keep` / `enhance`，并生成 Supabase 可直接执行的 SQL。

### 8.2 工具文件

| 文件 | 说明 |
|------|------|
| `scripts/gsc_label_urls_auto.mjs` | 主脚本 |
| `supabase/migrations/108_seo_gsc_urls_table.sql` | 数据库表结构 |
| `supabase/migrations/109_auto_label_gsc_urls.sql` | 自动标签 SQL |
| `supabase/migrations/110_apply_gsc_tags_to_pages.sql` | 批量更新页面 |

### 8.3 分类逻辑

#### delete（该删）

**触发条件**：
- URL 包含查询参数（`?`）
- Reason 包含 "soft 404"
- HTTP 状态码 404 或 410
- 内容字数 < 120

**处理方式**：
- 设置 `noindex = true`
- 设置 `in_sitemap = false`

#### keep（该留）

**触发条件**：
- Reason 包含 "discovered"（已发现未编入）
- Reason 包含 "crawled"（已抓取未编入）
- HTTP 状态码 >= 500（服务器错误，可能是临时问题）

**处理方式**：
- 无需处理，等待 Google 自然收录
- 持续监控状态

#### enhance（该增强）

**触发条件**：
- Reason 包含 "duplicate" 或 "canonical"
- 内容字数 120-250（过薄）
- Canonical URL 指向其他页面

**处理方式**：
- 增强内容差异化
- 添加行业特定内容
- 优化 FAQ 部分

### 8.4 使用流程

```bash
# 1. 从 GSC 导出 CSV
# GSC → Pages → 未编入索引 → 导出

# 2. 运行脚本
node scripts/gsc_label_urls_auto.mjs gsc_not_indexed.csv gsc_labeled.sql

# 3. 在 Supabase 执行 SQL
# Supabase → SQL Editor → 执行 gsc_labeled.sql

# 4. 批量更新页面
# 执行 110_apply_gsc_tags_to_pages.sql
```

---

## 九、GSC 冷启动 14 天行动表

### Day 0：初始化校验

- [ ] `/sitemap.xml` 返回 200 + XML
- [ ] `/sitemaps/tier1-0.xml` 有 URL
- [ ] GSC 提交 `/sitemaps/tier1-0.xml`
- [ ] URL Inspection 抽查 5 个

### Day 1-3：观察信号

**每天看**：
- Pages → 已发现 - 尚未编入索引（是否增长）
- Pages → 已编入索引（是否微增）
- Sitemaps 状态（只看成功/失败）

**正常表现**：
- 「已发现」上升
- 「已编入」小幅增长或不动
- Index 可能仍显示 0（正常）

### Day 4-7：逐步放量

**如果 Day 1-3 有增长**：
- 提交 `tier1-1.xml`
- 每天抽查 10 个 URL

**如果 Day 1-3 无增长**：
- 降低 chunk 大小
- 把重要 URL 放入 `sitemap-core.xml`

### Day 8-14：稳定放量

**每天看**：
- Discovered → Crawled → Indexed 流水线
- Crawl Stats 请求量
- Duplicate / Soft 404 是否爆发

**放量节奏**：
- 每天 tier2 +1-3 个 chunk
- 根据抓取反应调速

---

## 十、SEO 扩容 SOP

### 10.1 扩容准入条件

| 检查项 | 要求 | 阻断级别 |
|--------|------|----------|
| tier1-0 URL 数 | > 0 | FATAL |
| Index Rate | ≥ 40% | FATAL |
| Index Delta | ≥ 0（3日均） | FATAL |
| Duplicate Rate | < 20% | FATAL |
| 5xx Errors | < 10 (7d) | FATAL |
| Soft 404 | 不增长 | WARNING |

### 10.2 扩容步骤

```
1. 检查 Gate 状态
   └── BLOCKED → 停止，修复根因
   └── HOLD → 观察 3 天
   └── SAFE → 继续

2. 选择扩容目标
   └── 优先 Tier1（收录率高）
   └── 其次 Tier2（试错）

3. 小批量上线
   └── 每次 1-3 个 chunk
   └── 观察 24-48 小时

4. 监控指标
   └── Index Rate 不下降
   └── 5xx 不增加
   └── Duplicate 不爆发

5. 确认稳定后继续
```

### 10.3 5xx/404 紧急处理

| 状态 | 条件 | 动作 |
|------|------|------|
| 🔴 **RED** | 5xx ≥ 10 (7d) 或 5xx > 0 连续 3 天 | 立即冻结 |
| 🟡 **YELLOW** | 5xx > 0 (today) 或 404_in_sitemap > 0 | 冻结扩容，允许修复 |
| 🟢 **GREEN** | 5xx = 0 且 404_in_sitemap = 0 (连续 7 天) | 允许扩容 |

---

## 十一、个人身份运营指南

### 11.1 当前状态

```
身份：超体个体（Individual Operator）
收款：Stripe 一次性充值
模式：Prepaid Credits
能力：Enterprise 级（Preview 提供）
```

### 11.2 优势分析

| 优势 | 说明 |
|------|------|
| **法律风险低** | 无长期服务承诺 |
| **灵活性高** | 随时调整策略 |
| **成本低** | 无公司运营成本 |
| **升级空间** | 随时可注册公司 |

### 11.3 Credits 模式本质

```
Credits = 预付使用权

不是：
- 结果承诺
- 索引保证
- 流量承诺
```

---

## 十二、Enterprise Preview 策略

### 12.1 定位

```
"We're already operating at Enterprise standards.
We're just not forcing Enterprise contracts too early."
```

### 12.2 核心话术

**当客户问 Enterprise**：
```
Currently, Sora2 operates on a prepaid credits model.
For teams scaling seriously, we provide Enterprise-level 
capabilities in preview.
```

**当客户问公司**：
```
Sora2 is currently operated by me as an individual developer.
The systems are Enterprise-grade — that's how I built it.
Formal company registration happens when the scale requires it.
```

**当客户问能不能买**：
```
At this stage, Enterprise features are available case by case.
We start with credits, validate fit, and then discuss 
a formal Enterprise arrangement.
```

### 12.3 筛选姿态

```
你不是在"卖 Enterprise"
你是在"筛选未来的 Enterprise 客户"
```

### 12.4 Preview 不构成承诺

```
Preview features do not imply future availability or contractual obligation.
```

---

## 十三、法律文档体系

### 13.1 个人版 Terms of Service

**核心条款**：

1. **Operator 声明**
   ```
   Sora2 is operated by an individual developer.
   Services are provided on a best-effort basis.
   ```

2. **Credits 定义**
   ```
   Credits represent prepaid access to services, 
   not guaranteed outcomes.
   ```

3. **无结果保证**
   ```
   We do not guarantee any specific output, indexing, 
   ranking, or business results.
   ```

4. **责任上限**
   ```
   Liability cap = 3 months of payments
   ```

5. **服务可暂停**
   ```
   We reserve the right to limit or suspend services 
   to ensure system stability.
   ```

### 13.2 未来 Enterprise 合同结构

当注册公司后：

| 文档 | 用途 |
|------|------|
| **MSA** | 主服务协议 |
| **SLA** | 服务级别协议 |
| **SOW** | 具体工作说明 |
| **NDA** | 保密协议 |

---

## 十四、公司注册触发条件

### 14.1 必须注册（满足任一）

| 条件 | 标准 |
|------|------|
| Enterprise 客户要求 | 客户要求签合同 |
| 年收入 | ≥ $50,000 |
| 单客户金额 | ≥ $10,000 |
| 长期服务承诺 | 客户要求 SLA/年付 |
| 平台要求 | Stripe/银行要求主体 |
| 雇佣关系 | 任何员工/外包 |

### 14.2 建议注册

| 条件 | 缓冲期 |
|------|--------|
| 2+ 企业客户 | 30 天 |
| 外包/承包商 | 60 天 |
| 投资人沟通 | 沟通前完成 |
| 年收入 $30k-$50k | 提前准备 |

### 14.3 不需要注册

| 条件 | 状态 |
|------|------|
| 个人用户为主 | ✅ OK |
| 一次性充值 | ✅ OK |
| 无合同要求 | ✅ OK |
| 收入 < $30k | ✅ OK |

### 14.4 心理误判提醒

```
❗ "觉得自己应该注册公司" ≠ 触发条件

以下都不是注册理由：
- "感觉更专业"
- "别人都有公司"
- "万一以后需要"
- "有点焦虑"

这些是情绪，不是条件。
```

**自检问题**：
1. 有客户明确要求合同吗？
2. 年收入超过 $50k 了吗？
3. 单客户超过 $10k 了吗？
4. Stripe/银行要求了吗？
5. 要雇人了吗？

如果 5 个都是"没有" = 情绪驱动，不是条件驱动。

---

## 十五、数据库迁移清单

| Migration | 用途 | 状态 |
|-----------|------|------|
| `108_seo_gsc_urls_table.sql` | GSC URL 状态表 | ✅ |
| `109_auto_label_gsc_urls.sql` | 自动标签 SQL | ✅ |
| `110_apply_gsc_tags_to_pages.sql` | 批量更新页面 | ✅ |
| `112_sitemap_health_check.sql` | 健康检查表 | 待创建 |
| `113_seo_index_health_dashboard.sql` | Dashboard 视图 | 待创建 |

---

## 十六、脚本与工具清单

| 脚本 | 用途 | 状态 |
|------|------|------|
| `scripts/gsc_label_urls_auto.mjs` | GSC URL 自动标签 | ✅ |
| `scripts/sitemap_health_check.ts` | 每日健康检查 | 待创建 |
| `scripts/seo-scaling-gate.ts` | CI/CD Gate | 待创建 |
| `scripts/gsc_sitemap_check.sh` | curl 检查脚本 | ✅ |
| `scripts/verify_sitemap_fix.sh` | 修复验证脚本 | ✅ |

---

## 十七、文档索引

### 17.1 技术文档

| 文件 | 用途 |
|------|------|
| `docs/SITEMAP_FIX_2026_01_24.md` | 事故复盘 |
| `docs/SEO_INDEX_STATUS_2026_01_24.md` | 当前状态报告 |
| `docs/GSC_AUTO_LABELING_COMPLETE_GUIDE.md` | GSC 自动标签指南 |
| `docs/SORA2_SEO_INFRA_COMPLETE_GUIDE.md` | 完整 SEO 指南 |

### 17.2 销售文档

| 文件 | 用途 |
|------|------|
| `docs/ENTERPRISE_SALES_PPT.md` | 销售 PPT |
| `docs/ENTERPRISE_LANDING_PAGE.md` | Landing Page 文案 |
| `docs/ENTERPRISE_FAQ.md` | 客户 FAQ |
| `docs/SALES_DEMO_SCRIPT.md` | 演示脚本 |

### 17.3 法律文档

| 文件 | 用途 |
|------|------|
| `docs/legal/TERMS_OF_SERVICE_INDIVIDUAL.md` | 个人版 ToS |
| `docs/legal/ENTERPRISE_MSA_TEMPLATE.md` | MSA 模板 |
| `docs/legal/ENTERPRISE_SLA_TEMPLATE.md` | SLA 模板 |

### 17.4 投资人文档

| 文件 | 用途 |
|------|------|
| `docs/investor/MSA_SLA_CEO_SUMMARY.md` | CEO 摘要 |
| `docs/investor/INVESTOR_QA_HARDBALL.md` | 投资人 Q&A |

---

## 总结

### 你现在拥有的

```
┌─────────────────────────────────────────────────────────────────┐
│                    Sora2 SEO Infrastructure                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  技术层                                                          │
│  ├── 分层 Sitemap 架构（Core / Tier1 / Tier2）                   │
│  ├── Index Health Dashboard（核心指标监控）                       │
│  ├── 自动化健康检查（每日运行）                                   │
│  ├── CI/CD Gate（扩容门控）                                      │
│  └── GSC URL 自动标签（delete / keep / enhance）                 │
│                                                                 │
│  运营层                                                          │
│  ├── 14 天冷启动 Playbook                                        │
│  ├── 扩容 SOP（准入条件 + 回滚机制）                              │
│  └── Kill-Switch 机制                                           │
│                                                                 │
│  商业层                                                          │
│  ├── Enterprise Preview 策略                                    │
│  ├── 完整销售话术                                                │
│  └── 技术可信度白皮书                                            │
│                                                                 │
│  法律层                                                          │
│  ├── 个人版 Terms of Service                                    │
│  ├── MSA/SLA 模板（备用）                                        │
│  └── 公司注册触发条件                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 接下来要记住的 5 件事

1. **sitemap 必须从 tier1-0 开始**
2. **GSC 显示 0 ≠ 没抓取**
3. **SEO 扩容由 Index Gate 决定**
4. **credits 是唯一账本与收费单位**
5. **注册公司只在触发条件下发生**

### 一句话总结

```
从一个 sitemap off-by-one bug，
到完整的 Enterprise 级 SEO Infrastructure，
再到个人身份合规运营方案。

技术、商业、法律三者完全一致。
随时可以升级，但不被迫升级。
```

---

*文档版本: 1.0 (Baseline) | 创建时间: 2026-01-24*
