# GSC URL 样例收集（用于优化 SQL 规则）

> **目的**：收集你站点的真实 URL 样例，优化 path_type/slug 提取规则到 100% 命中

---

## 📋 需要提供的 3 个 URL 样例

请提供以下 3 个真实 URL（从你的站点复制粘贴即可）：

### 1. Use Cases 页面 URL
```
例如：https://sora2aivideos.com/use-cases/anime-video-generator
```

### 2. Country / Locale 页面 URL（如果有）
```
例如：https://sora2aivideos.com/country/us/video-generator
或：https://sora2aivideos.com/us/video-generator
或：其他格式
```

### 3. Blog / Templates / Scenes 页面 URL（你最核心的）
```
例如：https://sora2aivideos.com/blog/sora-vs-runway
或：https://sora2aivideos.com/templates/xxx
或：https://sora2aivideos.com/scenes/xxx
```

---

## 🎯 提供样例后的优化

拿到你的 3 个真实 URL 后，我会：

1. **分析路径结构**
   - 确认路径分隔符
   - 确认 slug 位置
   - 确认多级路径规则

2. **优化 SQL 规则**
   - 更新 `110_apply_gsc_tags_to_pages.sql` 中的 path_type 映射
   - 优化 slug 提取逻辑
   - 确保 100% 命中你的项目结构

3. **提供最终版本**
   - 可直接覆盖的 SQL 文件
   - 完全匹配你的路由结构

---

## 📝 当前支持的路径类型（基于代码库分析）

根据代码库分析，当前 SQL 已支持：

- ✅ `/use-cases/{slug}` → `use_cases` 表
- ✅ `/blog/{slug}` → 需要确认表名
- ✅ `/keywords/{slug}` → 需要确认表名
- ✅ `/industries/{slug}` → 需要确认表名
- ✅ `/compare/{slug}` → 需要确认表名
- ⚠️ `/country/{cc}/{slug}` → 需要确认是否存在

**如果你没有 country 页面**，可以提供其他主要路径类型的 URL。

---

**请直接粘贴 3 个真实 URL 到聊天窗口即可** 🚀
