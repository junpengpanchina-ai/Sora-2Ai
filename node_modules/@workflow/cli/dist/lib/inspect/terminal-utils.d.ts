/**
 * Utilities for terminal display and environment detection
 */
/**
 * Check if running in CI environment
 */
export declare const isCI: () => boolean;
/**
 * Check if running in an interactive terminal
 */
export declare const isInteractive: () => boolean;
/**
 * Get the current terminal width
 */
export declare const getTerminalWidth: () => number;
/**
 * Calculate display settings based on terminal width
 */
export interface DisplaySettings {
    showRelativeDates: boolean;
    dataFieldWidth: number;
    abbreviateStatus: boolean;
    namesTruncateLength: number;
    truncateIdsToLastChars: boolean;
    hideCompletedAt: boolean;
}
export declare const getDisplaySettings: (terminalWidth: number, withData: boolean, isRunTable?: boolean) => DisplaySettings;
/**
 * Format status with optional abbreviation
 */
export declare const formatStatus: (status: string, abbreviated: boolean) => string;
/**
 * Format ISO date without T and Z characters
 */
export declare const formatISODate: (date: Date) => string;
//# sourceMappingURL=terminal-utils.d.ts.map