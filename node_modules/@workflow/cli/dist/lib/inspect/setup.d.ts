import { createWorld } from '@workflow/core/runtime';
/**
 * Setup CLI world configuration.
 * If throwOnConfigError is false, will return null world with the error message
 * instead of throwing, allowing the web UI to open for configuration.
 */
export declare const setupCliWorld: (flags: {
    json: boolean;
    verbose: boolean;
    backend: string;
    env: string;
    authToken: string;
    project: string;
    team: string;
}, version: string, ignoreLocalWorldConfigError?: boolean) => Promise<Awaited<ReturnType<typeof createWorld>> | null>;
//# sourceMappingURL=setup.d.ts.map